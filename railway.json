{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm ci --prefer-offline --no-audit && npm run build:api"
  },
  "deploy": {
    "startCommand": "npm start"
  }
}
